# 璇玑 AgentFlow 2.0 重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将当前技术原型重构为可通过本机及多台远程 Hermes 真实执行DAG任务的Tauri桌面控制台。

**Architecture:** Tauri负责桌面壳和本地进程生命周期；Python Coordinator负责项目、工作流、调度、节点、执行、文件和恢复；Hermes Node Agent负责封装每台机器上的Hermes任务生命周期；React前端只保留单一无限画布工作区。

**Tech Stack:** Tauri 2、React 19、TypeScript、ReactFlow、Python 3.11、FastAPI、SQLite、pytest、Vitest、Playwright。

---

## 执行约束

1. 项目当前不是Git仓库。Task 1先初始化Git并提交原型基线。
2. 每个任务必须先写测试或验证脚本，再实现，再运行完整相关测试。
3. 不把API密钥、SSH私钥、token提交到Git。
4. 不再使用Stub执行成功冒充真实执行。
5. 每个Milestone结束必须得到一个可运行、可验证的闭环。

---

### Task 1：建立安全基线与真实状态说明

**Files:**
- Create: `.gitignore`
- Create: `README.md`
- Create: `docs/CURRENT_STATE.md`
- Modify: `07-项目进展报告.md`

- [ ] 添加忽略规则：`backend/.env`、`backend/*.db`、`.venv/`、`node_modules/`、`dist/`、`src-tauri/target/`、项目产出目录。
- [ ] 将07报告标记为“历史原型报告，完成度声明无效”，引用08设计规范。
- [ ] README明确开发启动命令、架构、真实已实现和未实现功能。
- [ ] 初始化Git并提交原型基线。
- [ ] 验证：`git status --short`不显示密钥、数据库、构建目录。

### Task 2：建立后端测试框架和领域Schema

**Files:**
- Create: `backend/pyproject.toml`
- Create: `backend/src/xuanji/domain/models.py`
- Create: `backend/src/xuanji/domain/enums.py`
- Create: `backend/tests/test_domain_models.py`
- Create: `backend/tests/test_workflow_validation.py`

- [ ] 定义 Project、Workflow、Task、Run、TaskAttempt、HermesNode、Artifact。
- [ ] 定义任务和运行状态枚举。
- [ ] 编写DAG无环、缺失依赖、重复ID、非法状态转换的失败测试。
- [ ] 实现Pydantic Schema和校验器。
- [ ] 验证：`pytest backend/tests/test_domain_models.py backend/tests/test_workflow_validation.py -q`。

### Task 3：重构SQLite存储和迁移

**Files:**
- Create: `backend/src/xuanji/storage/database.py`
- Create: `backend/src/xuanji/storage/migrations.py`
- Create: `backend/src/xuanji/storage/repositories.py`
- Create: `backend/tests/test_storage.py`
- Retire: `backend/storage.py`

- [ ] 创建projects、workflows、tasks、runs、task_attempts、nodes、artifacts、events表。
- [ ] 使用显式事务；Workflow与Run状态分离。
- [ ] 添加schema_version及迁移测试。
- [ ] 编写创建项目、保存版本、恢复运行、事件分页测试。
- [ ] 验证：`pytest backend/tests/test_storage.py -q`。

### Task 4：实现项目目录与Artifact Manager

**Files:**
- Create: `backend/src/xuanji/artifacts/manager.py`
- Create: `backend/src/xuanji/artifacts/manifest.py`
- Create: `backend/tests/test_artifacts.py`

- [ ] 创建项目、workflow、run、task目录。
- [ ] 防止路径穿越。
- [ ] 生成SHA-256、大小、媒体类型清单。
- [ ] 只有文件校验成功后才可标记任务success。
- [ ] 验证：`pytest backend/tests/test_artifacts.py -q`。

### Task 5：重写规划器

**Files:**
- Create: `backend/src/xuanji/planner/service.py`
- Create: `backend/src/xuanji/planner/providers.py`
- Create: `backend/src/xuanji/planner/prompts.py`
- Create: `backend/tests/test_planner.py`
- Retire: `backend/planner.py`, `backend/model_config.py`

- [ ] 抽象DeepSeek/MiMo OpenAI兼容Provider。
- [ ] 使用结构化Schema解析规划结果。
- [ ] 规划结果执行DAG校验并提供一次自动修复。
- [ ] API密钥只从环境或系统安全存储读取。
- [ ] 用MockTransport测试成功、非法JSON、环依赖、Provider失败和fallback。
- [ ] 验证：`pytest backend/tests/test_planner.py -q`。

### Task 6：实现Hermes Node协议和Fake Node

**Files:**
- Create: `backend/src/xuanji/nodes/protocol.py`
- Create: `backend/src/xuanji/nodes/registry.py`
- Create: `backend/src/xuanji/nodes/client.py`
- Create: `node-agent/app.py`
- Create: `node-agent/executor.py`
- Create: `backend/tests/fakes/fake_node.py`
- Create: `backend/tests/test_node_registry.py`

- [ ] 定义health、capabilities、create task、status、cancel、logs、artifacts接口。
- [ ] 实现本机和远程节点注册、健康检查、超时和认证头。
- [ ] Fake Node支持可控成功、失败、延迟、取消和文件产出。
- [ ] Node Agent先封装Hermes CLI one-shot执行，并明确进程ID和工作目录。
- [ ] 验证：`pytest backend/tests/test_node_registry.py -q`。

### Task 7：实现调度器和状态机

**Files:**
- Create: `backend/src/xuanji/scheduler/state_machine.py`
- Create: `backend/src/xuanji/scheduler/readiness.py`
- Create: `backend/src/xuanji/scheduler/scoring.py`
- Create: `backend/src/xuanji/scheduler/service.py`
- Create: `backend/tests/test_scheduler.py`
- Retire: `backend/scheduler.py`

- [ ] 实现ready条件、拓扑调度和并发限制。
- [ ] 实现能力过滤、负载评分、模型匹配和节点偏好。
- [ ] 实现幂等派发、重试等待、取消、跳过和blocked。
- [ ] 消除无ready任务时无限等待的死锁。
- [ ] 验证：`pytest backend/tests/test_scheduler.py -q`。

### Task 8：实现Execution Manager与恢复

**Files:**
- Create: `backend/src/xuanji/execution/manager.py`
- Create: `backend/src/xuanji/execution/recovery.py`
- Create: `backend/tests/test_execution_integration.py`
- Retire: `backend/executor.py`, `backend/monitor.py`, `backend/collector.py`

- [ ] 创建异步后台Run，不让HTTP start请求阻塞到全部结束。
- [ ] 轮询/订阅节点状态并持久化事件。
- [ ] 拉回产出并通过Artifact Manager验证。
- [ ] Coordinator重启后对账所有非终态任务。
- [ ] 取消请求必须调用Node cancel。
- [ ] 验证：Fake Node完整执行、失败重试、取消、恢复、artifact失败。

### Task 9：重写Coordinator API与事件流

**Files:**
- Create: `backend/src/xuanji/api/app.py`
- Create: `backend/src/xuanji/api/projects.py`
- Create: `backend/src/xuanji/api/workflows.py`
- Create: `backend/src/xuanji/api/runs.py`
- Create: `backend/src/xuanji/api/nodes.py`
- Create: `backend/src/xuanji/api/events.py`
- Create: `backend/tests/test_api.py`
- Retire: `backend/main.py`

- [ ] 按08规范实现REST接口。
- [ ] 使用lifespan代替deprecated on_event。
- [ ] 返回正确HTTP状态码和结构化错误。
- [ ] WebSocket事件带event_id，支持从last_event_id补发。
- [ ] 验证：`pytest backend/tests/test_api.py -q`。

### Task 10：重构前端为单一画布Shell

**Files:**
- Create: `app/src/app/AppShell.tsx`
- Create: `app/src/features/canvas/WorkflowCanvas.tsx`
- Create: `app/src/features/canvas/nodeTypes.ts`
- Create: `app/src/features/inspector/Inspector.tsx`
- Create: `app/src/features/projects/ProjectRail.tsx`
- Create: `app/src/features/runs/RunBar.tsx`
- Create: `app/src/store/workspaceStore.ts`
- Create: `app/src/domain/types.ts`
- Modify: `app/src/App.tsx`
- Remove after migration: `app/src/pages/Home.tsx`, `Input.tsx`, `Plan.tsx`, `Edit.tsx`, `Run.tsx`, `Result.tsx`, `app/src/panels/*`

- [ ] 先写Vitest组件测试：Shell渲染、选择节点、运行栏状态。
- [ ] 只保留项目资源栏+顶部运行栏+中央画布+右侧检查器。
- [ ] 所有业务状态放统一store，不使用sessionStorage。
- [ ] 画布节点使用真实Task定义，不使用阶段面板。
- [ ] 正确使用ReactFlow type-only imports，`npm run build`必须通过。

### Task 11：实现工作流编辑与审核

**Files:**
- Create: `app/src/features/canvas/nodes/TaskNode.tsx`
- Create: `app/src/features/canvas/nodes/SystemNode.tsx`
- Create: `app/src/features/inspector/TaskEditor.tsx`
- Create: `app/src/features/workflow/ReviewGate.tsx`
- Create: `app/src/features/workflow/__tests__/workflow-edit.test.tsx`

- [ ] 支持任务增删改、连线和断开。
- [ ] 支持依赖校验和环错误提示。
- [ ] 每个节点有清晰主操作按钮。
- [ ] 审核前禁止执行全部；审核后冻结一个Workflow版本。
- [ ] 验证前端测试和浏览器交互。

### Task 12：实现节点管理UI和SSH Provisioning

**Files:**
- Create: `backend/src/xuanji/provisioning/ssh.py`
- Create: `backend/src/xuanji/provisioning/service.py`
- Create: `backend/tests/test_provisioning.py`
- Create: `app/src/features/nodes/NodeManager.tsx`
- Create: `app/src/features/nodes/ProvisionWizard.tsx`

- [ ] SSH私钥只保存路径。
- [ ] SSH执行doctor、安装、部署Node Agent、启动、健康检查。
- [ ] 每一步输出可见日志和失败原因。
- [ ] 测试使用Fake SSH runner，不连接用户服务器。
- [ ] 用户明确提供服务器后才执行真实远程安装。

### Task 13：实现实时监控与产出UI

**Files:**
- Create: `app/src/features/runs/useRunEvents.ts`
- Create: `app/src/features/runs/TaskLog.tsx`
- Create: `app/src/features/artifacts/ArtifactBrowser.tsx`
- Create: `app/src/features/runs/__tests__/events.test.tsx`

- [ ] WebSocket断线重连并从last_event_id补发。
- [ ] 节点实时显示派发、执行、收集和失败状态。
- [ ] 点击任务查看日志与真实产出文件。
- [ ] 支持重试、取消、跳过并显示服务器返回。

### Task 14：Tauri管理Coordinator生命周期

**Files:**
- Modify: `app/src-tauri/src/lib.rs`
- Create: `app/src-tauri/src/coordinator.rs`
- Modify: `app/src-tauri/tauri.conf.json`
- Create: `app/src/lib/runtime.ts`

- [ ] Dev模式启动已有Python venv服务。
- [ ] Prod模式启动打包后的Coordinator sidecar。
- [ ] 健康检查后再渲染工作区。
- [ ] 端口冲突时选择可用端口并注入前端。
- [ ] 退出应用时优雅停止Coordinator。
- [ ] 修正图标，删除未使用Rust import。

### Task 15：端到端测试与生产打包

**Files:**
- Create: `app/playwright.config.ts`
- Create: `app/e2e/local-workflow.spec.ts`
- Create: `app/e2e/multi-node.spec.ts`
- Create: `scripts/verify-all.sh`

- [ ] 用Fake Node跑通输入→规划→编辑→审核→执行→文件→汇总。
- [ ] 测试多节点自动分配。
- [ ] 测试取消、失败重试、节点离线、Coordinator重启。
- [ ] 运行：后端pytest、前端test、lint、tsc、Vite build、Cargo check、Tauri build。
- [ ] 双击产出的`.app`，验证不依赖Vite和手动启动后端。
- [ ] 生成`.dmg`并记录真实文件路径和大小。

### Task 16：清理、文档与发布候选

**Files:**
- Rewrite: `07-项目进展报告.md`
- Create: `docs/USER_GUIDE.md`
- Create: `docs/NODE_DEPLOYMENT.md`
- Create: `docs/TROUBLESHOOTING.md`
- Remove: 过时原型与重复页面文件

- [ ] 只根据测试证据更新完成度。
- [ ] 文档说明本机Hermes发现、远程节点部署、工作流执行和产出路径。
- [ ] 删除死代码、重复CSS、旧原型数据库和缓存构建产物。
- [ ] 最终验证 `scripts/verify-all.sh` 退出码为0。
