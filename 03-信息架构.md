# 璇玑 — 信息架构

## 页面地图

```
璇玑
├── 首页（历史运行列表）
├── 新建运行
│   └── 输入页 → 规划页 → 编排页 → 执行页 → 结果页
├── 历史运行详情
└── 设置
```

## 6个核心页面

| # | 页面 | 功能 | 对应模块 |
|---|------|------|---------|
| 1 | **首页** | 运行历史列表 + 新建入口 | 存储器 |
| 2 | **输入页** | 选题输入 + 上下文补充 | 输入器 |
| 3 | **规划页** | AI思考过程 + DAG预览 | 规划器 |
| 4 | **编排页** | DAG可视化编辑 + 审核确认 | 编排器 |
| 5 | **执行页** | 实时监控 + 操作控制 | 调度器+执行器+监视器 |
| 6 | **结果页** | 产出汇总 + 导出 | 收集器+导出器 |

---

## 页面详细结构

### 1. 首页

```
┌──────────────────────────────────────────┐
│  璇玑                              [设置] │
├──────────────────────────────────────────┤
│                                          │
│  [+ 新建运行]                             │
│                                          │
│  最近运行                                 │
│  ┌──────────────────────────────────────┐│
│  │ 📋 跨境电商SaaS市场分析   ✅ 07-27   ││
│  │ 📋 工厂战争游戏设计       ✅ 07-26   ││
│  │ 📋 AI编排器竞品调研       ✅ 07-25   ││
│  └──────────────────────────────────────┘│
│                                          │
└──────────────────────────────────────────┘
```

**包含元素**：
- 新建运行按钮（主要操作）
- 历史运行列表（时间排序）
- 每条记录：标题 + 状态图标 + 日期
- 点击进入对应运行的结果页
- 设置入口（右上角）

---

### 2. 输入页

```
┌──────────────────────────────────────────┐
│  ← 新建运行                               │
├──────────────────────────────────────────┤
│                                          │
│  你想做什么？                              │
│  ┌──────────────────────────────────────┐│
│  │                                      ││
│  │  输入你的选题或目标...                 ││
│  │                                      ││
│  └──────────────────────────────────────┘│
│                                          │
│  ▶ 高级选项                               │
│  ┌──────────────────────────────────────┐│
│  │ 背景信息：[                          ]││
│  │ 约束条件：[                          ]││
│  │ 执行引擎：(●) Hermes  (○) 手动导出    ││
│  └──────────────────────────────────────┘│
│                                          │
│                    [开始规划 →]            │
│                                          │
└──────────────────────────────────────────┘
```

**包含元素**：
- 主输入框（大文本域，必填）
- 高级选项（可展开/收起）
  - 背景信息（补充上下文）
  - 约束条件（格式/语言/长度）
  - 执行引擎选择
- 开始规划按钮

---

### 3. 规划页

```
┌──────────────────────────────────────────┐
│  ← 新建运行              跳过规划直接编辑  │
├────────────────────┬─────────────────────┤
│                    │                     │
│  AI思考过程         │   DAG预览           │
│  ┌──────────────┐  │   ┌─────────────┐  │
│  │              │  │   │             │  │
│  │  正在分析...  │  │   │  任务图预览  │  │
│  │              │  │   │  (只读)      │  │
│  │  🔄 理解领域  │  │   │             │  │
│  │  ✓ 梳理知识  │  │   │  [A] → [C]  │  │
│  │  🔄 拆解板块  │  │   │  [B] ↗      │  │
│  │  ⏳ 规划依赖  │  │   │             │  │
│  │              │  │   │             │  │
│  └──────────────┘  │   └─────────────┘  │
│                    │                     │
├────────────────────┴─────────────────────┤
│  Token消耗: 2,340  │  耗时: 12s          │
│                    [确认进入编排 →]        │
└──────────────────────────────────────────┘
```

**包含元素**：
- 左侧：AI思考过程（流式输出，步骤打勾动画）
- 右侧：DAG预览（只读，等规划完成后渲染）
- 底部：Token消耗 + 耗时
- 操作：确认进入编排 / 跳过直接编辑

---

### 4. 编排页 ⭐ 核心页面

```
┌──────────────────────────────────────────┐
│  ← 返回规划  │  运行: 跨境电商SaaS分析    │
├──────────────────────────────────────────┤
│  工具栏: [添加任务] [连线模式] [版本]      │
├──────────────────────────┬───────────────┤
│                          │               │
│     DAG编辑器             │   节点详情     │
│     (ReactFlow)          │   面板        │
│                          │               │
│  [市场调研]──┐            │  标题: [    ] │
│              ├→[数据整合] │  描述: [    ] │
│  [竞品分析]──┘     │      │  类型: [ ▼ ]  │
│                    │      │  依赖: [    ] │
│                    ▼      │               │
│               [报告生成]   │  ───────────  │
│                          │  AI思考逻辑:   │
│                          │  "因为需要先   │
│                          │   了解市场才能  │
│                          │   做竞品对比"  │
│                          │               │
├──────────────────────────┴───────────────┤
│  审核进度: ██░░░ 2/5 板块已审核            │
│  [跳过未审核] [确认执行 →]                 │
└──────────────────────────────────────────┘
```

**包含元素**：
- 顶部工具栏：添加任务、连线模式、版本切换
- 主区域：ReactFlow DAG编辑器
  - 节点颜色按Agent类型区分
  - 可拖拽、可连线
- 右侧：选中节点的详情面板
  - 编辑表单（标题/描述/类型/依赖）
  - AI思考逻辑展示
- 底部：审核进度 + 操作按钮

**交互**：
- 点击节点 → 右侧显示详情
- 拖拽节点 → 调整位置
- 拖拽连线 → 设置依赖
- 双击节点 → 快速编辑
- 右键 → 删除/复制

---

### 5. 执行页

```
┌──────────────────────────────────────────┐
│  ← 返回编排  │  运行中: 跨境电商SaaS分析   │
├──────────────────────────────────────────┤
│                                          │
│  整体进度: ████████░░░░ 65%               │
│  预计剩余: 约4分钟                         │
│                                          │
├──────────────────────────────────────────┤
│                                          │
│  任务状态                                 │
│  ┌──────────────────────────────────────┐│
│  │ ✅ 市场调研      Research    3m12s    ││
│  │ ✅ 竞品分析      Research    5m45s    ││
│  │ 🔄 数据整合      Business    运行中    ││
│  │    └─ 正在清洗Shopify数据...          ││
│  │ ⏳ 报告生成      Code        等待中    ││
│  └──────────────────────────────────────┘│
│                                          │
├──────────────────────────────────────────┤
│  实时日志                                 │
│  ┌──────────────────────────────────────┐│
│  │ 10:01:02 [INFO] 开始整合市场数据      ││
│  │ 10:01:05 [INFO] 加载任务1产出        ││
│  │ 10:01:08 [INFO] 加载任务2产出        ││
│  │ 10:01:12 [INFO] 数据清洗完成         ││
│  └──────────────────────────────────────┘│
│                                          │
│  [暂停] [终止]                            │
└──────────────────────────────────────────┘
```

**包含元素**：
- 整体进度条 + 预计剩余时间
- 任务列表：状态图标 + 名称 + 类型 + 耗时
  - 运行中任务显示当前步骤
- 实时日志面板（自动滚动）
- 操作按钮：暂停/恢复/终止
  - 点击任务可展开：重试/跳过/修改

---

### 6. 结果页

```
┌──────────────────────────────────────────┐
│  ← 返回首页  │  跨境电商SaaS分析   ✅ 完成 │
├──────────────────────────────────────────┤
│  [导出全部] [复制报告] [新建运行]          │
├──────────────────────────┬───────────────┤
│                          │               │
│  任务产出列表             │   产出详情     │
│                          │               │
│  ✅ 市场调研 ──→ [点击查看]│  # 市场调研    │
│  ✅ 竞品分析 ──→ [点击查看]│               │
│  ✅ 数据整合 ──→ [点击查看]│  | 指标 | 值 | │
│  ✅ 报告生成 ──→ [点击查看]│  | ---- | -- | │
│                          │  | ...  | ...| │
│                          │               │
├──────────────────────────┴───────────────┤
│  📋 汇总报告                              │
│  ┌──────────────────────────────────────┐│
│  │ 完整的Markdown报告内容...             ││
│  └──────────────────────────────────────┘│
└──────────────────────────────────────────┘
```

**包含元素**：
- 顶部操作：导出全部 / 复制报告 / 新建运行
- 左侧：任务产出列表（点击切换）
- 右侧：选中任务的产出详情
- 底部：汇总报告区域
- 导出选项：Markdown / PDF / JSON

---

## 用户流程图

### 主流程

```
首页
  │
  │ 点击「新建运行」
  ▼
输入页
  │ 输入选题
  │ 点击「开始规划」
  ▼
规划页
  │ AI思考中...（等待）
  │ 思考完成，查看DAG
  │ 点击「确认进入编排」
  ▼
编排页
  │ 编辑DAG、逐板块审核
  │ 点击「确认执行」
  ▼
执行页
  │ 实时监控进度
  │ 等待完成
  ▼
结果页
  │ 查看产出、导出
  │ 完成
  ▼
首页（运行记录出现在列表中）
```

### 分支流程

```
规划页
  │
  ├── 满意 → 进入编排
  │
  └── 不满意 → 重新规划 / 局部重规划

编排页
  │
  ├── 手动执行 → 导出prompt → 复制粘贴
  │                 │
  │                 └── 粘贴结果回来 → 结果页
  │
  └── 自动执行 → 进入执行页

执行页
  │
  ├── 正常完成 → 结果页
  │
  ├── 任务失败 → 弹出操作菜单
  │     ├── 重试 → 继续执行
  │     ├── 跳过 → 继续执行
  │     └── 终止 → 结果页（部分完成）
  │
  └── 用户暂停 → 等待恢复
```

---

## 导航结构

```
全局导航（左上角返回）
  └── 任何页面可返回上一级

页面层级：
  首页
    └── 新建运行
          ├── 输入页
          ├── 规划页
          ├── 编排页
          ├── 执行页
          └── 结果页
    └── 历史运行详情（= 结果页）

底部固定（仅执行页）
  └── 操作按钮区
```

## 页面间跳转规则

| 从 | 到 | 条件 |
|----|-----|------|
| 首页 | 输入页 | 点击新建 |
| 首页 | 结果页 | 点击历史记录 |
| 输入页 | 规划页 | 点击开始规划 |
| 规划页 | 编排页 | 点击确认 |
| 规划页 | 编排页 | 点击跳过 |
| 编排页 | 执行页 | 点击确认执行 |
| 编排页 | 结果页 | 导出手动执行 |
| 执行页 | 结果页 | 任务全部完成 |
| 结果页 | 首页 | 点击返回首页 |
| 结果页 | 输入页 | 点击新建运行 |
