# 安全策略

## 支持范围

安全修复优先覆盖默认分支和最新公开版本。历史安装包仅用于回溯，不代表仍受支持。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私密入口。如果该入口不可用，请通过维护者的 GitHub 账号联系，并只提供最少的必要信息。

请不要在公开 Issue、Pull Request、日志或截图中提交以下内容：

- API Key、Node Token、SSH 私钥或访问令牌；
- `credentials.json`、数据库或真实用户数据；
- 可直接利用的完整攻击代码，直到维护者确认可以公开。

报告应尽量包含受影响版本/提交、复现步骤、影响范围、临时缓解措施和安全联系人可回信的方式。维护者会先确认收到报告，再评估严重性、修复方案和公开时间；这不是即时响应承诺。

## 安全边界

- Planner Key 和 Node Token 只允许保存在本地用户配置中，不应由 API 回传。
- SSH 连接必须启用主机密钥校验（`StrictHostKeyChecking=yes`）。
- 本地开发、测试和发布包中的凭据必须使用占位值或 Mock，不得使用真实账号。

本策略不替代法律、合规或运营响应流程；高影响事件应同时通知实际系统所有者。
