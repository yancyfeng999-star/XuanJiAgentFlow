# 璇玑 AgentFlow 2.0 — 重构设计规范

> 版本：2.0 Draft
> 日期：2026-07-27
> 状态：已确认，进入重构实施

---

## 1. 产品定义

### 1.1 一句话定位

**璇玑是一个本地运行的分布式 AI 任务控制台：独立规划任务，在无限画布上人工审核，并将任务自动调度到本机或多台远程 Hermes 执行。**

### 1.2 第一版成功标准

用户能够完成一条真实闭环：

```text
输入目标
→ DeepSeek/MiMo 生成任务 DAG
→ 在无限画布编辑、审核
→ 注册本机与远程 Hermes 节点
→ 调度器自动选择节点
→ Hermes 真实执行
→ 实时查看状态与日志
→ 产出文件同步回项目目录
→ 下游任务按文件路径读取上游产出
→ 所有节点完成并生成最终交付目录
```

### 1.3 不做的事情

第一版不做：

- 多租户与团队权限
- 云端中央控制平台
- Kubernetes 或复杂消息队列集群
- 运行中任务跨节点迁移
- Codex/WorkBuddy 自动适配
- 模板市场与商业计费

---

## 2. 产品交互

### 2.1 单一核心工作区

产品只保留一个核心交互模型：**无限画布**。

不再维护“传统多页流程”和“画布流程”两套业务界面。设置、节点管理、历史记录可以使用抽屉或独立管理页，但任务创建、编排、执行、监控与结果全部发生在同一个项目画布内。

### 2.2 画布节点类型

画布只承载真正的工作流节点，不在画布上模拟页面。

#### 系统节点

- 目标输入节点
- 文件输入节点
- 人工审核关卡
- 条件分支节点
- 最终汇总节点

#### 任务节点

每个任务节点展示：

- 标题与 Agent 类型
- 当前状态
- 执行节点
- 输入文件数量
- 产出文件数量
- 重试次数
- 主操作按钮

```text
┌──────────────────────────────┐
│ 市场规模调研       Research   │
│ 状态：等待执行                │
│ 执行节点：自动选择             │
│ 输入：0  产出：market.md       │
│                              │
│ [编辑] [复制指令] [执行]       │
└──────────────────────────────┘
```

### 2.3 固定界面区域

#### 顶部运行栏

- 项目名称
- 工作流版本
- 当前运行状态
- 总进度
- 在线 Hermes 节点数
- 审核、执行全部、暂停、终止

#### 左侧资源栏

- 当前工作流
- 历史版本
- 运行记录
- 产出文件
- Hermes 节点
- 设置

#### 右侧检查器

点击节点后统一显示：

- 任务描述与 Prompt
- Agent 类型
- 输入依赖与文件
- 调度约束
- 执行节点
- 超时与重试策略
- 实时日志
- 产出文件
- 取消、重试、跳过

---

## 3. 系统架构

```text
┌─────────────────────────────────────────────┐
│             璇玑 Tauri Desktop              │
│                                             │
│  React 无限画布                             │
│  ├── 项目/工作流状态                        │
│  ├── DAG 编辑器                             │
│  ├── Hermes 节点管理                        │
│  └── 日志/产出/控制                         │
│                     │ Tauri IPC / localhost │
│  Local Coordinator                         │
│  ├── Planner                                │
│  ├── Scheduler                              │
│  ├── Node Registry                          │
│  ├── Execution Manager                      │
│  ├── Artifact Manager                       │
│  ├── Recovery Manager                       │
│  └── SQLite                                 │
└─────────────────────┬───────────────────────┘
                      │
          ┌───────────┼────────────┐
          │           │            │
       本机节点     远程节点 A    远程节点 B
       Hermes       Hermes        Hermes
          │           │            │
        API         HTTPS API    HTTPS API
                      ▲
                      └── SSH：安装、更新、启停、诊断
```

### 3.1 Tauri 职责

- 启动、监控、停止 Local Coordinator
- 选择 SSH 私钥路径
- 操作系统通知
- 项目目录选择与打开
- 应用更新
- 不承载业务调度逻辑

### 3.2 Local Coordinator 职责

- 规划器调用 DeepSeek/MiMo
- 项目、工作流、任务、运行的领域管理
- Hermes 节点注册与健康检查
- DAG 调度和节点选择
- 任务生命周期与恢复
- 文件产出同步和索引
- REST/WebSocket API

### 3.3 Hermes Node 职责

- 对外提供受认证的节点 API
- 启动真实 Hermes 任务
- 保存日志与文件产出
- 支持状态查询和取消
- 上报能力、负载和健康状态

如果 Hermes 官方 API Server 不能提供完整的异步任务生命周期，第一版由一个轻量 Node Agent 封装 Hermes CLI/API，而不是在 Coordinator 中假设不存在的接口。

---

## 4. 领域模型

### 4.1 Project

一个用户目标对应一个项目目录。

```text
Project
├── id
├── name
├── root_path
├── active_workflow_version
├── created_at
└── updated_at
```

### 4.2 Workflow

```text
Workflow
├── id
├── project_id
├── version
├── goal
├── planner_provider
├── planner_model
├── status: draft/reviewed/archived
└── graph_json
```

### 4.3 Task

```text
Task
├── id
├── workflow_id
├── title
├── description
├── prompt
├── agent_type
├── dependencies[]
├── execution_policy
├── retry_policy
├── expected_outputs[]
└── ui_position
```

### 4.4 Run / TaskAttempt

Workflow 是静态定义，Run 是一次执行，TaskAttempt 是任务的一次尝试。不能再把静态任务和运行状态混在同一张表里。

```text
Run
├── id
├── workflow_id
├── status
├── started_at
└── completed_at

TaskAttempt
├── id
├── run_id
├── task_id
├── node_id
├── attempt
├── status
├── started_at
├── completed_at
├── error
└── result_manifest
```

### 4.5 HermesNode

```text
HermesNode
├── id
├── name
├── kind: local/remote
├── api_url
├── ssh_host
├── ssh_port
├── ssh_user
├── ssh_key_path
├── status
├── capabilities_json
├── max_concurrency
├── running_tasks
├── success_rate
└── last_seen_at
```

SSH 私钥只保存路径，不复制私钥内容。API 令牌后续使用系统钥匙串；第一版不得明文写入项目数据库。

---

## 5. 项目文件结构

```text
XuanjiProjects/
└── project_xxx/
    ├── project.json
    ├── workflow/
    │   ├── v001.json
    │   └── v002.json
    ├── runs/
    │   └── run_xxx/
    │       ├── manifest.json
    │       └── tasks/
    │           └── task_xxx/
    │               ├── instruction.md
    │               ├── result.json
    │               ├── logs.jsonl
    │               └── artifacts/
    ├── shared/
    └── deliverables/
```

### 5.1 上下游传递

上游结果不整体塞入下游 Prompt。下游任务接收产出清单：

```json
{
  "inputs": [
    {
      "task_id": "task_001",
      "path": "runs/run_xxx/tasks/task_001/artifacts/market-size.md",
      "media_type": "text/markdown"
    }
  ]
}
```

远程节点完成后，产出必须同步回本机项目目录并校验文件大小和哈希，之后才能把任务标记为成功。

---

## 6. 节点连接与安装

### 6.1 本机 Hermes

启动时：

1. 检测 Hermes CLI
2. 检测已登录配置
3. 检测 API/Node Agent
4. 没安装时提供一键安装
5. 安装后运行 doctor 和健康检查

### 6.2 远程 Hermes

SSH 只负责控制面操作：

- 检测系统与依赖
- 安装/升级 Hermes
- 部署/升级 Node Agent
- 启停服务
- 读取诊断日志

日常任务执行使用受认证的 HTTPS API，不通过长时间 SSH 会话执行。

### 6.3 节点能力

```json
{
  "id": "node_sg_01",
  "status": "online",
  "max_concurrency": 10,
  "running_tasks": 3,
  "models": ["gpt-5.6-sol", "mimo-v2.5-pro"],
  "tools": ["web", "terminal", "file", "browser"],
  "tags": ["research", "linux", "public-network"],
  "latency_ms": 83
}
```

---

## 7. 调度规则

### 7.1 可运行条件

任务只有在以下条件全部满足时才能进入 ready：

- 所有依赖任务成功或按策略跳过
- 输入文件已同步到项目目录
- DAG 无环
- 至少一个在线节点满足能力要求
- 项目未暂停或取消

### 7.2 节点评分

```text
节点得分 =
  能力匹配 × 35%
+ 当前负载 × 25%
+ 模型匹配 × 20%
+ 网络延迟 × 10%
+ 历史成功率 × 10%
```

任务支持：

- 自动分配
- 固定节点
- 限定节点组
- 本机优先
- 远程优先

### 7.3 状态机

```text
pending → ready → dispatching → running → collecting → success
                 ↘ dispatch_failed              ↘ artifact_failed
running → cancelling → cancelled
running → failed → retry_wait → ready
pending/ready → skipped
任意非终态 → blocked
```

取消必须真正取消远程Hermes任务，而不是只修改数据库状态。

---

## 8. API 与事件

### 8.1 Coordinator REST

```text
POST   /projects
GET    /projects
GET    /projects/{id}

POST   /projects/{id}/plan
PUT    /workflows/{id}
POST   /workflows/{id}/validate
POST   /workflows/{id}/runs

GET    /runs/{id}
POST   /runs/{id}/pause
POST   /runs/{id}/resume
POST   /runs/{id}/cancel

POST   /runs/{id}/tasks/{task_id}/retry
POST   /runs/{id}/tasks/{task_id}/skip

GET    /nodes
POST   /nodes/local/discover
POST   /nodes/remote/provision
POST   /nodes/{id}/diagnose
DELETE /nodes/{id}

GET    /runs/{id}/artifacts
```

### 8.2 WebSocket 事件

事件必须带递增序号，支持断线重连后从 `last_event_id` 补发：

```text
run.created
run.status_changed
task.ready
task.dispatched
task.started
task.log
task.artifact
task.completed
task.failed
node.status_changed
```

---

## 9. 错误处理与恢复

- Coordinator 启动后扫描所有非终态 Run
- 向节点对账真实任务状态
- 节点离线时任务进入 blocked，不伪造失败
- 执行请求使用幂等键，防止重复派发
- 远程任务完成但文件同步失败时进入 artifact_failed
- 所有异常使用结构化错误码，不返回裸字符串
- 规划器返回必须经过 Schema 校验、DAG无环校验和自动修复

---

## 10. 测试策略

### 后端单元测试

- 规划结果 Schema 与DAG校验
- 拓扑排序、并行层、环检测
- 节点评分和能力过滤
- 状态机合法/非法转换
- 重试、取消、暂停与恢复
- 文件清单、路径安全、哈希校验

### 后端集成测试

- SQLite迁移与事务
- Fake Hermes Node任务生命周期
- WebSocket事件顺序和重连补发
- 远程文件同步失败
- Coordinator崩溃重启恢复

### 前端测试

- 画布节点增删改和连线
- 右侧检查器编辑
- 审核与执行按钮状态
- WebSocket事件驱动UI
- 节点离线/任务失败/重试

### E2E

- 输入目标→规划→编辑→执行→文件产出→汇总
- 本机节点
- 一台远程Fake节点
- 多节点自动分配
- 中途取消
- Coordinator重启恢复

### Tauri验证

- 首次启动
- Coordinator自动启动与退出
- 后端端口冲突
- 生产构建不依赖Vite
- `.app`/`.dmg`双击可用

---

## 11. 旧代码处置

### 保留并重构

- Tauri壳
- ReactFlow依赖
- DeepSeek/MiMo规划调用
- SQLite
- 设计Token
- Prompt导出概念

### 删除

- 画布阶段面板模型（输入面板/规划面板/编排面板/执行面板/结果面板串联）
- 与画布重复的多页业务流程
- sessionStorage业务状态
- Stub执行器自动成功
- 未验证的 `/api/delegate` 假设
- 虚假完成度文档

### 重写

- 领域数据模型
- Scheduler和Execution Manager
- Hermes节点适配器
- WebSocket事件模型
- Canvas工作区
- Tauri与Coordinator生命周期
- 项目文件产出模型

---

## 12. 分阶段交付

### Milestone 1：本机真实闭环

- 单一无限画布
- DeepSeek/MiMo规划
- DAG审核编辑
- 本机Hermes发现
- 一个任务真实执行
- 日志与文件产出
- 下游触发

### Milestone 2：远程单节点

- SSH检测、安装、升级、诊断
- Node Agent API
- 远程任务执行
- 文件回传

### Milestone 3：多节点调度

- 多节点注册
- 能力与负载评分
- 并行派发
- 节点离线与重试

### Milestone 4：稳定与发布

- 崩溃恢复
- 全量测试
- Tauri生产打包
- `.dmg`安装验证
