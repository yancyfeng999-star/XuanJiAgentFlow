# 璇玑 — 设计交付

> Phase ⑥：开发者交付包，让前端开发能直接照着实现

## 交付物清单

| 文件 | 说明 | 路径 |
|------|------|------|
| 设计系统 | 色彩/字体/间距/组件规范 | 05-设计系统.md |
| 高保真原型 | 6个页面交互原型 | prototype/index.html |
| 页面组件拆分 | 每个页面拆成哪些React组件 | 本文档 |
| CSS变量映射 | 原型CSS → 项目CSS变量 | 本文档 |
| API接口清单 | 前端需要对接的API | 02-技术架构.md |

---

## 页面 → 组件拆分

### 1. 首页 (Home)

```
App
├── Sidebar                    # 左侧导航
│   ├── SidebarLogo            # Logo + 版本号
│   ├── SidebarNav             # 导航菜单项
│   └── SidebarFooter          # 底部信息
├── TopBar                     # 顶部栏
│   ├── TopBarTitle            # 标题
│   └── TopBarActions          # 右侧操作按钮
├── HomePage                   # 首页内容
│   ├── HomeHero               # 标语 + 新建按钮
│   └── RunList                # 运行历史列表
│       └── RunItem            # 单条运行记录
└── Content                    # 内容滚动区
```

### 2. 输入页 (Input)

```
InputPage
├── InputHeader                # 返回 + 标题
├── InputMain                  # 主输入区
│   ├── InputTitle             # "你想做什么？"
│   ├── InputTextarea          # 大文本输入框
│   ├── AdvancedToggle         # 高级选项展开/收起
│   └── AdvancedPanel          # 高级选项面板
│       ├── BackgroundInput    # 背景信息
│       ├── ConstraintRow      # 格式+语言选择
│       └── EngineRadio        # 执行引擎选择
└── InputActions               # 开始规划按钮
```

### 3. 规划页 (Plan)

```
PlanPage
├── PlanHeader                 # 返回 + 标题 + 跳过/确认按钮
├── PlanSplit                  # 左右分栏
│   ├── PlanLeft               # 左栏：AI思考
│   │   ├── PlanSectionTitle   # "AI 思考过程"
│   │   └── ThinkSteps         # 步骤列表
│   │       └── ThinkStep      # 单个步骤（图标+标题+描述）
│   └── PlanRight              # 右栏：DAG预览
│       ├── PlanSectionTitle   # "DAG 预览"
│       ├── DagPreview         # DAG预览区
│       │   └── MiniDag        # 迷你DAG节点图
│       └── PlanStats          # Token + 耗时
```

### 4. 编排页 (Edit) ⭐ 最复杂

```
EditPage
├── EditHeader                 # 返回 + 标题 + 审核进度 + 确认按钮
├── EditLayout                 # 左右布局
│   ├── EditCanvas             # 左侧：ReactFlow画布
│   │   ├── EditToolbar        # 浮动工具栏（添加/连线/版本）
│   │   ├── CanvasNodes        # 节点层
│   │   │   └── CanvasNode     # 单个节点（可拖拽）
│   │   └── CanvasArrows       # SVG连线层
│   └── EditPanel              # 右侧：详情面板
│       ├── PanelHeader        # "节点详情"
│       ├── PanelBody          # 表单区
│       │   ├── PanelField     # 表单项（标题/描述/类型/耗时）
│       │   ├── PanelDivider   # 分割线
│       │   ├── DependencyBadges # 依赖标签
│       │   └── PanelThink     # AI思考逻辑展示
```

### 5. 执行页 (Run)

```
RunPage
├── RunHeader                  # 返回 + 标题 + 状态Badge
├── RunMonitor                 # 监控区
│   ├── RunProgressSection     # 进度区
│   │   ├── RunProgressHeader  # "整体进度" + 预计剩余
│   │   ├── RunProgressBar     # 进度条
│   │   └── RunProgressStats   # 百分比 + 任务数
│   ├── TaskList               # 任务列表
│   │   └── TaskItem           # 单个任务（图标+信息+状态+耗时）
│   ├── LogPanel               # 日志面板
│   │   ├── LogHeader          # "实时日志"
│   │   └── LogBody            # 日志内容
│   │       └── LogLine        # 单行日志（时间+级别+消息）
│   └── RunActions             # 操作按钮（暂停/终止）
```

### 6. 结果页 (Result)

```
ResultPage
├── ResultHeader               # 返回 + 标题 + 状态 + 操作按钮
├── ResultLayout               # 左右布局
│   ├── ResultSidebar          # 左侧：任务列表
│   │   └── ResultTaskItem     # 单个任务（可点击切换）
│   └── ResultMain             # 右侧：产出详情
│       └── ResultContent      # 产出内容卡片
│           ├── ResultTable    # 数据表格
│           └── ResultText     # Markdown文本内容
```

---

## CSS变量映射

原型中使用的CSS变量直接映射到项目中：

```css
/* globals.css - 项目全局样式 */
:root {
  /* 主色 */
  --primary: #6366F1;
  --primary-hover: #4F46E5;
  --primary-light: #EEF2FF;

  /* 中性色 */
  --bg: #F8F9FC;
  --surface: #FFFFFF;
  --border: #E5E7EB;
  --border-light: #F3F4F6;
  --text-primary: #111827;
  --text-secondary: #6B7280;
  --text-tertiary: #9CA3AF;

  /* Agent类型色 */
  --research: #10B981;
  --research-bg: #ECFDF5;
  --code: #3B82F6;
  --code-bg: #EFF6FF;
  --business: #F59E0B;
  --business-bg: #FFFBEB;
  --review: #8B5CF6;
  --review-bg: #F5F3FF;

  /* 状态色 */
  --success: #10B981;
  --success-bg: #ECFDF5;
  --running: #3B82F6;
  --running-bg: #EFF6FF;
  --pending: #9CA3AF;
  --pending-bg: #F3F4F6;
  --failed: #EF4444;
  --failed-bg: #FEF2F2;
  --paused: #F59E0B;
  --paused-bg: #FFFBEB;

  /* 圆角 */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;

  /* 阴影 */
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.06);
  --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
  --shadow-lg: 0 12px 24px rgba(0,0,0,0.12);

  /* 字体 */
  --font-sans: 'Inter', 'Noto Sans SC', -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}
```

---

## 组件库依赖

| 组件 | 来源 | 用途 |
|------|------|------|
| Button | shadcn/ui | 所有按钮 |
| Input | shadcn/ui | 文本输入框 |
| Textarea | shadcn/ui | 多行输入 |
| Select | shadcn/ui | 下拉选择 |
| Badge | shadcn/ui | 状态徽章 |
| Card | shadcn/ui | 卡片容器 |
| Progress | shadcn/ui | 进度条 |
| RadioGroup | shadcn/ui | 单选按钮 |
| Tabs | shadcn/ui | 版本切换 |
| ScrollArea | shadcn/ui | 滚动区域 |
| ReactFlow | reactflow | DAG编辑器 |
| Lucide React | lucide-react | 所有图标 |

---

## 开发顺序建议

按页面复杂度和依赖关系排序：

```
Week 1: 基础设施
  ├── 项目脚手架（Vite + React + TS）
  ├── CSS变量 + 全局样式
  ├── Sidebar组件
  ├── TopBar组件
  └── 页面路由

Week 2: 简单页面
  ├── 首页（Home）
  ├── 输入页（Input）
  └── 结果页（Result）

Week 3: 核心页面
  ├── 规划页（Plan）
  └── 编排页（Edit）← 最复杂，ReactFlow集成

Week 4: 执行+收尾
  ├── 执行页（Run）
  ├── WebSocket集成
  └── 状态管理
```
