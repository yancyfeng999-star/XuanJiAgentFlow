# 更新与反馈

启动时**不会**检查、下载或安装更新。

桌面版只有一个「检查更新」入口（设置页按钮与原生菜单共用）：

1. 没有新版本：提示已是最新。
2. 有新版本：下载 → 关闭软件 → 静默安装 → 重新打开。
3. 有任务正在运行 / 暂停 / 取消中：不关软件，提示结束后再检查。

浏览器开发态只提示“仅桌面版可检查更新”。反馈请使用 GitHub Issue 模板，并先脱敏。
