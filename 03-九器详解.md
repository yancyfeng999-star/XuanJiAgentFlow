# 璇玑 — 九器详解

## 总览

| 序号 | 器 | 职责 | 阶段 |
|------|-----|------|------|
| 1 | **输入器** | 接收选题，补充上下文 | 起始 |
| 2 | **规划器** | AI思考+拆解→DAG | 规划 |
| 3 | **编排器** | DAG可视化+人工审核 | 编排 |
| 4 | **调度器** | 解析依赖+分发执行+状态流转 | 执行 |
| 5 | **执行器** | 调Hermes API执行任务 | 执行 |
| 6 | **导出器** | 生成prompt文本供手动执行 | 执行 |
| 7 | **监视器** | 实时展示执行进度+状态 | 监控 |
| 8 | **收集器** | 汇总所有任务产出 | 收尾 |
| 9 | **存储器** | 持久化项目数据+历史记录 | 全程 |

---

## 1. 输入器

**职责**：接收用户选题，引导补充必要上下文。

### 输入

| 字段 | 必填 | 说明 |
|------|------|------|
| 选题 | ✅ | 自然语言描述目标 |
| 背景 | ❌ | 补充上下文信息 |
| 约束 | ❌ | 产出格式、语言、长度等要求 |
| 引擎 | ❌ | 执行方式：自动(Hermes) / 手动(导出) |

### 处理

1. 解析用户输入
2. 识别缺失的关键信息
3. 必要时追问补充
4. 组装完整上下文传给规划器

### 输出

```json
{
  "goal": "调研跨境电商SaaS市场并生成报告",
  "context": "需要2024-2025年数据，关注Shopify/BigCommerce/Shoplazza",
  "constraints": {
    "format": "markdown报告",
    "language": "中文",
    "length": "5000字以上"
  },
  "engine": "hermes"
}
```

### UI

- 简洁输入框
- 可选展开「高级选项」（背景/约束/引擎选择）
- 支持追加上下文

---

## 2. 规划器

**职责**：AI深度思考，将选题拆解为结构化任务DAG。

### 输入

输入器输出的完整上下文。

### 处理流程

```
┌─ 理解阶段 ─────────────────────────┐
│  · 这个选题是什么领域？              │
│  · 涉及哪些知识点？                 │
│  · 目标受众是谁？                   │
│  · 产出应该是什么形态？              │
└────────────────────────────────────┘
    │
    ▼
┌─ 梳理阶段 ─────────────────────────┐
│  · 需要哪些前置调研？               │
│  · 可以分成几个板块？               │
│  · 板块间的逻辑关系是什么？          │
│  · 哪些是核心板块，哪些是辅助？      │
└────────────────────────────────────┘
    │
    ▼
┌─ 拆解阶段 ─────────────────────────┐
│  · 每个板块下拆成具体任务            │
│  · 每个任务标注Agent类型             │
│  · 每个任务预估耗时                  │
│  · 标注产出格式                      │
└────────────────────────────────────┘
    │
    ▼
┌─ 编排阶段 ─────────────────────────┐
│  · 分析依赖关系                      │
│  · 标注可并行的任务组                │
│  · 确定执行顺序                      │
│  · 生成完整DAG                       │
└────────────────────────────────────┘
```

### 输出（DAG JSON）

```json
{
  "id": "run_abc123",
  "goal": "调研跨境电商SaaS市场并生成报告",
  "thinking": "这个领域涉及...，需要先了解...，核心板块是...",
  "nodes": [
    {
      "id": "task_1",
      "title": "市场概况调研",
      "description": "调研全球跨境电商SaaS市场规模、趋势",
      "agent_type": "research",
      "status": "pending",
      "dependencies": [],
      "estimated_time": "5min",
      "output_format": "markdown表格"
    }
  ],
  "parallel_groups": [["task_1", "task_2"]],
  "created_at": "2026-07-27T10:00:00Z",
  "status": "planning"
}
```

### 重新规划

- **全量重规划**：整个DAG推翻，AI重新思考
- **局部重规划**：只重拆某个板块，其他保持不变

### Agent类型定义

| 类型 | 颜色 | 图标 | 用途 |
|------|------|------|------|
| Code Agent | 蓝色 | </> | 写代码、调试、部署 |
| Research Agent | 绿色 | 🔍 | 联网搜索、读文档、分析 |
| Business Agent | 橙色 | ⚡ | API调用、数据处理、爬虫 |
| Review Agent | 紫色 | ✓ | 检查产出质量 |

### UI

- 左侧：AI思考过程展示（流式输出）
- 右侧：DAG预览（只读，等待审核）
- 底部：规划耗时、Token消耗

---

## 3. 编排器

**职责**：DAG可视化编辑，人工逐板块审核确认。

### 输入

规划器输出的DAG JSON。

### 功能

| 功能 | 说明 |
|------|------|
| 可视化 | ReactFlow渲染任务图，节点+连线 |
| 审核 | 点击节点查看AI思考逻辑（为什么这么拆） |
| 编辑 | 修改标题、描述、Agent类型 |
| 调整依赖 | 拖拽连线改变依赖关系 |
| 增加任务 | 新增板块或子任务 |
| 删除任务 | 移除不需要的节点 |
| 标记跳过 | 标记已完成/不需要执行的任务 |
| 版本管理 | 保存多个规划版本做对比 |

### 审核流程

```
用户逐板块审核
    │
    ├── ✅ 确认 → 标记该板块为已审核
    ├── ✏️ 编辑 → 修改内容后确认
    ├── 🗑️ 删除 → 移除该板块/任务
    ├── ➕ 新增 → 添加额外任务
    └── 🔄 重拆 → 让AI重新规划该板块
    │
    ▼
全部审核通过 → 点击「确认执行」
```

### UI

- 主区域：ReactFlow DAG编辑器
- 右侧面板：选中节点的详情（AI思考逻辑+编辑表单）
- 顶部工具栏：添加/删除/保存/版本切换
- 底部状态栏：审核进度（X/Y 板块已审核）

---

## 4. 调度器 ⭐ 核心

**职责**：解析DAG依赖，并行/串行分发执行，管理状态流转。

### 输入

编排器确认后的DAG。

### 处理流程

```
扫描DAG
    │
    ├── 找出所有 dependencies=[] 的任务 → 第一批并行执行
    │
    ├── 等待第一批全部完成
    │
    ├── 找出依赖已全部完成的任务 → 第二批并行执行
    │
    ├── 重复直到所有任务完成
    │
    └── 汇总结果 → 交给收集器
```

### 执行示例

```
任务1(无依赖) ─┐
                ├→ 任务3(依赖1+2) ─→ 任务4(依赖3)
任务2(无依赖) ─┘

调度器执行：
  第1轮：并行 → 任务1 + 任务2
  第2轮：串行 → 任务3（等1、2完成）
  第3轮：串行 → 任务4（等3完成）
```

### 状态机

```
pending → claimed → running → success
                          ↓
                       failed → retry → running
                          ↓
                       timeout
                          ↓
                       blocked（等待人工干预）
                          ↓
                       skipped
```

### 并发控制

| 参数 | 默认值 | 说明 |
|------|--------|------|
| max_parallel | 5 | 最大并行任务数 |
| task_timeout | 300s | 单任务超时（秒） |
| max_retries | 3 | 失败重试次数 |

### 结果传递

上游任务完成后，其产出自动注入到下游任务的上下文中：

```json
{
  "task_id": "task_3",
  "input": {
    "task_1_output": "...任务1的产出...",
    "task_2_output": "...任务2的产出..."
  }
}
```

### 失败处理

| 策略 | 说明 |
|------|------|
| 自动重试 | 失败后自动重试，最多max_retries次 |
| 跳过 | 标记为skipped，继续执行下游（下游可处理缺失） |
| 人工介入 | 阻塞，等待用户决策（暂停/重试/修改/终止） |
| 终止 | 终止整个运行 |

### UI

- 实时更新DAG节点状态（颜色/图标变化）
- 点击节点查看当前状态详情
- 操作按钮：暂停/恢复/重试/跳过/终止

---

## 5. 执行器

**职责**：调用Hermes API执行具体任务。

### 输入

调度器分发的执行指令 + 任务上下文。

### 处理

```
收到执行指令
    │
    ├── 组装Hermes API请求
    │     ├── goal: 任务描述
    │     ├── context: 前序任务产出
    │     ├── agent_type: Agent类型
    │     └── timeout: 超时设置
    │
    ├── POST http://localhost:8001/api/delegate
    │
    ├── 轮询/WebSocket 获取状态
    │
    └── 返回结果给调度器
```

### API调用格式

```python
POST http://localhost:8001/api/delegate
{
    "tasks": [
        {
            "goal": "调研Shopify市场数据",
            "context": "需要2024-2025年的市场份额、用户增长、收入数据",
            "agent_type": "research"
        }
    ]
}
```

### 后续扩展

| 引擎 | 接入方式 | 阶段 |
|------|---------|------|
| Hermes | HTTP API | MVP |
| Codex | CLI/API | 后续 |
| WorkBuddy | 适配器 | 后续 |

---

## 6. 导出器

**职责**：生成可复制粘贴的prompt文本，供外部工具执行。

### 输入

调度器分发的执行指令 + DAG结构。

### 输出格式

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
璇玑任务导出 | 运行ID: run_abc123
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━ 任务 1/4 ━━━━━━━━━━━━━━━━━━━━━━
类型：Research Agent
依赖：无（可立即执行）
──────────────────────────────────
目标：调研全球跨境电商SaaS市场规模
要求：
  - 2024-2025年数据
  - 市场增长率
  - 主要玩家份额
产出格式：Markdown表格
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━ 任务 2/4（可与任务1并行）━━━━━
类型：Research Agent
依赖：无（可立即执行）
──────────────────────────────────
目标：调研Shopify产品功能和定价
要求：
  - 最新套餐价格
  - 核心功能列表
  - 与竞品差异
产出格式：Markdown表格
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━ 任务 3/4（等任务1、2完成后执行）━━
类型：Business Agent
依赖：任务1 + 任务2
──────────────────────────────────
目标：整合市场数据和竞品分析
输入参考：
  [任务1产出]
  [任务2产出]
产出格式：结构化数据
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━ 任务 4/4（等任务3完成后执行）━━━
类型：Code Agent
依赖：任务3
──────────────────────────────────
目标：生成完整分析报告
输入参考：
  [任务3产出]
产出格式：完整Markdown报告（5000字+）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 并行标记

可并行的任务用「可与任务X并行」标注，用户可同时复制给多个窗口。

### 导出选项

| 选项 | 说明 |
|------|------|
| 全部导出 | 所有任务一次性导出 |
| 按轮次导出 | 只导出当前可执行的任务 |
| 单任务导出 | 只导出选中的任务 |

---

## 7. 监视器

**职责**：实时展示执行进度、状态、日志。

### 数据来源

- 调度器状态更新
- 执行器实时日志
- WebSocket推送

### 监控面板

```
┌──────────────────────────────────────┐
│  璇玑运行中：跨境电商市场调研           │
├──────────────────────────────────────┤
│                                      │
│  整体进度：████████░░░░ 60%           │
│                                      │
│  任务状态：                           │
│  ✅ 任务1 市场概况    完成 (3m12s)     │
│  ✅ 任务2 竞品分析    完成 (5m45s)     │
│  🔄 任务3 数据整合    运行中 (2m)      │
│  ⏳ 任务4 报告生成    等待中           │
│                                      │
├──────────────────────────────────────┤
│  实时日志：                           │
│  10:01:02 [INFO] 开始整合市场数据      │
│  10:01:05 [INFO] 加载任务1产出        │
│  10:01:08 [INFO] 加载任务2产出        │
│  10:01:12 [INFO] 数据清洗完成         │
└──────────────────────────────────────┘
```

### 功能

| 功能 | 说明 |
|------|------|
| 进度条 | 整体完成百分比 |
| 状态列表 | 每个任务的实时状态 |
| 日志流 | 实时滚动日志 |
| 耗时统计 | 每个任务+整体耗时 |
| 操作按钮 | 暂停/恢复/重试/跳过/终止 |

### 通信方式

- WebSocket实时推送
- 心跳检测（每5秒）
- 断线自动重连

---

## 8. 收集器

**职责**：汇总所有任务产出，生成完整结果。

### 输入来源

| 来源 | 方式 |
|------|------|
| Hermes自动执行 | 直接从API返回结果 |
| 手动执行 | 用户粘贴产出到对应任务 |
| 混合模式 | 部分自动+部分手动 |

### 处理

```
所有任务完成（success/skipped）
    │
    ├── 按DAG顺序汇总所有产出
    │
    ├── 按任务类型分类整理
    │     ├── Research产出 → 调研数据
    │     ├── Business产出 → 处理结果
    │     ├── Code产出 → 代码/报告
    │     └── Review产出 → 质量评分
    │
    ├── 生成汇总报告（可选）
    │
    └── 存入存储器
```

### 输出

| 格式 | 说明 |
|------|------|
| 分任务查看 | 每个任务的独立产出 |
| 汇总视图 | 所有产出整合在一起 |
| 导出文件 | Markdown/PDF/JSON |

### UI

- 左侧：任务列表（可点击切换）
- 右侧：选中任务的产出详情
- 顶部：汇总导出按钮

---

## 9. 存储器

**职责**：持久化所有数据，支持历史浏览和复用。

### 存储内容

| 数据 | 位置 | 说明 |
|------|------|------|
| 运行记录 | SQLite runs表 | 每次运行的基本信息 |
| 任务数据 | SQLite tasks表 | 每个任务的状态/结果/日志 |
| DAG版本 | SQLite + JSON文件 | 每次规划的DAG快照 |
| 产出文件 | 文件系统 | Agent产出的原始文件 |
| 配置 | YAML文件 | 用户设置/引擎配置 |

### 历史浏览

```
所有运行
  ├── run_abc123 (2026-07-27) ✅ 已完成
  ├── run_def456 (2026-07-26) ✅ 已完成
  └── run_ghi789 (2026-07-25) ❌ 失败
```

### 复用能力

| 功能 | 说明 |
|------|------|
| 复制规划 | 基于历史DAG创建新运行 |
| 模板保存 | 将成功的规划保存为模板 |
| 导入导出 | DAG的JSON导入/导出 |

### 数据清理

| 数据 | 保留策略 |
|------|---------|
| 运行记录 | 永久 |
| 产出文件 | 30天（可配置） |
| 日志 | 7天（可配置） |
